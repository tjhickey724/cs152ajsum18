extends layoutBS

block content
  h1= title
  p Welcome to authDemo 
  | user=#{user}
  if loggedIn 
    p You are logged in 
    a(href="/logout") Logout
  else 
    p You are not logged in
    a(href="/auth/google") Login 
